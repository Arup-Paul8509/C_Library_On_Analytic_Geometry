#include "Library.c"
double find_angle(struct Equation,struct Equation);
double find_distance(struct Location,struct Location);
double find_area(struct Coordinate,struct Coordinate,struct Coordinate);